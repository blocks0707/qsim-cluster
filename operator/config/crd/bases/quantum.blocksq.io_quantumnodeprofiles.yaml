---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: quantumnodeprofiles.quantum.blocksq.io
spec:
  group: quantum.blocksq.io
  names:
    categories:
    - quantum
    kind: QuantumNodeProfile
    listKind: QuantumNodeProfileList
    plural: quantumnodeprofiles
    singular: quantumnodeprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.pool
      name: Pool
      type: string
    - jsonPath: .spec.cpu.cores
      name: CPU
      type: integer
    - jsonPath: .spec.memory.totalGB
      name: Memory
      type: string
    - jsonPath: .spec.gpu.available
      name: GPU
      type: boolean
    - jsonPath: .status.currentLoad.cpuUsagePercent
      name: Load
      type: string
    - jsonPath: .status.currentLoad.activeJobs
      name: Active Jobs
      type: integer
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: QuantumNodeProfile is the Schema for the quantumnodeprofiles
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: QuantumNodeProfileSpec defines the desired state of QuantumNodeProfile
            properties:
              cpu:
                description: CPU capabilities
                properties:
                  architecture:
                    default: x86_64
                    description: CPU architecture
                    enum:
                    - x86_64
                    - arm64
                    type: string
                  cores:
                    description: Number of CPU cores
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - cores
                type: object
              gpu:
                description: GPU capabilities
                properties:
                  available:
                    description: Whether GPU is available
                    type: boolean
                  count:
                    description: Number of GPUs
                    format: int32
                    type: integer
                  memoryGB:
                    description: GPU memory in GB
                    format: int32
                    type: integer
                  type:
                    description: GPU type/model (e.g., "A100", "V100")
                    type: string
                required:
                - available
                type: object
              memory:
                description: Memory capabilities
                properties:
                  totalGB:
                    description: Total memory in GB
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - totalGB
                type: object
              pool:
                description: Node pool this node belongs to
                enum:
                - auto
                - cpu
                - high-cpu
                - gpu
                type: string
              simulatorConfig:
                description: Quantum simulator configuration
                properties:
                  maxConcurrentJobs:
                    default: 3
                    description: Maximum concurrent jobs this node can handle
                    format: int32
                    minimum: 1
                    type: integer
                  supportedMethods:
                    description: Supported simulation methods
                    items:
                      enum:
                      - statevector
                      - stabilizer
                      - mps
                      - automatic
                      type: string
                    type: array
                type: object
            required:
            - cpu
            - gpu
            - memory
            - pool
            type: object
          status:
            description: QuantumNodeProfileStatus defines the observed state of QuantumNodeProfile
            properties:
              conditions:
                description: Conditions represent the latest observations of node
                  state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentLoad:
                description: Current resource load and usage
                properties:
                  activeJobs:
                    description: Current number of active quantum jobs
                    format: int32
                    minimum: 0
                    type: integer
                  cpuUsagePercent:
                    description: Current CPU usage percentage (0-100)
                    maximum: 100
                    minimum: 0
                    type: number
                  memoryUsagePercent:
                    description: Current memory usage percentage (0-100)
                    maximum: 100
                    minimum: 0
                    type: number
                required:
                - activeJobs
                - cpuUsagePercent
                - memoryUsagePercent
                type: object
              lastUpdated:
                description: Last time the status was updated
                format: date-time
                type: string
              ready:
                description: Whether the node is ready to accept quantum jobs
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
